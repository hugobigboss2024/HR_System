src/
├── api/             <-- 放 Axios 請求（例如 auth.js, employee.js）
├── assets/          <-- 放圖片、Sass 全局樣式
├── components/      <-- 通用 UI 元件
├── layout/          <-- Workstem 框架版面 (Header + Sidebar)
├── router/          <-- Vue Router 路由配置
├── stores/          <-- Pinia 狀態管理 (User, Company, Token)
└── views/           <-- 各個功能頁面 (Dashboard, Employee, Leave)